# Backlog

The single source of truth for outstanding work, plans, and notes lives in
[**PROJECT_BACKLOG.md**](./PROJECT_BACKLOG.md).

This stub exists only so anyone reaching for the old name finds the new one.
Do not add content here — add it to `PROJECT_BACKLOG.md` instead.
