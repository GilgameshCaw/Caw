{
  "name": "caw-verifier",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Standalone CAW mirror verifier — checks that a mirror's FE bundle and on-chain contracts match the canonical upstream repo.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react-swc": "^3.5.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
