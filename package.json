{
  "name": "caw-protocol",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "stop": "cd client && npm run stop",
    "start": "cd client && npm start",
    "test": "cd client && npm test",
    "web": "cd client && npm run web"
  },
  "devDependencies": {
    "prisma": "~6.18.0"
  },
  "dependencies": {
    "@prisma/client": "~6.18.0"
  }
}
